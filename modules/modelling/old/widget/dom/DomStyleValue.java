package lib.kasuga.widget.dom;

import lib.kasuga.widget.dom.style.StyleValue;

public class DomStyleValue<R> extends StyleValue<DomElement, R> {
    public DomStyleValue(R initialValue) {
        super(initialValue);
    }
}
